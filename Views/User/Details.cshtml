@model MyMvcApp.Models.User

<h2>User Details</h2>

@if (Model != null)
{
    <dl>
        <dt>ID</dt>
        <dd>@Model.Id</dd>
        <dt>Name</dt>
        <dd>@Model.Name</dd>
        <dt>Email</dt>
        <dd>@Model.Email</dd>
    </dl>
}
else
{
    <p>User not found.</p>
}